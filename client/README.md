# Relay311 test client

From this directory, run `npm install` and then `npm run dev`. The Vite dev server proxies `/api` requests to `http://localhost:3000`, so start the Relay311 backend separately on that port.

Run `npm run build` for a production build.
